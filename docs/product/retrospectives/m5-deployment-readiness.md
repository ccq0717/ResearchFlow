# M5 部署就绪审查

> 结论：本地实现与部署配置已就绪；M5 尚需真实容器和 Railway 线上验收

## 四轴结论

1. **实现质量**：修复原失败运行可重复或并发重试、会话令牌缺少服务端过期校验、版本不兼容时仍可能建表、API Dockerfile 复制顺序错误；新增对应测试。
2. **代码—文档一致性**：生产环境强制真实工作流、公开 CORS 和访问码；路线图只勾选已实现内容，部署指南明确区分“已准备”和“已上线”。
3. **设计演进**：首次部署确定为 Railway Hobby、Singapore、两个服务、API 单实例和 1 GB Volume；继续保留 SQLite 与进程内运行边界，不引入账号系统、队列或 PostgreSQL。
4. **简洁性**：访问入口从 Dashboard 提取为独立组件，删除请求复用统一客户端；部署指南从平台通用规划收敛为实际 Railway 参数，课程只解释当前项目所需概念且没有练习。

## 已验证

- 后端测试、Ruff、前端单测、ESLint 和生产构建通过；
- Chromium E2E 覆盖访问码、创建、完成、报告、度量与刷新恢复；
- 本地 SQLite 与 uploads 的备份—恢复演练通过；
- 文档本地链接有效，常规测试不读取真实 `.env` 或调用外部服务。

## 真实上线前剩余工作

- 在安装 Docker 的环境实际构建两个镜像并验证 Compose；
- 在 Railway 创建项目、服务、Secret、Volume、域名、预算提醒和定时备份；
- 用真实 Provider 完成线上黄金流程、重启持久化、恢复与回滚验收；
- 验收后补充真实链接、截图和短演示视频。

这些步骤依赖部署账号和平台资源；完成前不把 M5 或在线 Demo 标记为完成。
