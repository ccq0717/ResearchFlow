# 进入 M5 前审查：M0～M4 成品基线

> 日期：2026-08-27  
> 结论：阻塞问题已修复，可以进入 M5

## 实现质量

- 完整测试暴露出关闭后台任务后立即写终态可能触发 SQLite 写锁。关闭流程现只取消并等待任务；下次启动通过既有恢复逻辑把遗留运行标记为 `RUN_INTERRUPTED`，避免在连接释放阶段追加写入。
- 后端 33 项测试连续运行 8 轮均通过；Ruff、前端测试、ESLint 和生产构建通过。
- `LangGraphResearchWorkflow` 与研究仓储文件较长，但各自仍围绕单一工作流或聚合仓储职责。当前不按节点或表机械拆分，后续只在职责出现第二个变化原因时提取模块。

## 实现与文档一致性

- 将 Exa 能力准确描述为 Provider 返回的网页 highlights，而不是完整网页抓取；
- 修正 Gemini Embedding 2 的文本任务指令描述；
- 将早期 MVP 规格明确标为历史验收基线；
- 修正报告事件遗漏本地证据的文案。

## 设计演进

M5 范围仍然成立，实施顺序调整为：任务生命周期可靠性 → 记录管理 → E2E 与度量 → 安全和容器化 → 在线部署与作品集材料。SQLite、单实例 API 和进程内任务继续作为作品集 Demo 的明确约束，不提前引入队列或分布式基础设施。

## 简洁性

- README 只保留项目价值、当前能力、最短启动路径和关键安全边界，详细配置与排错集中到运维手册；
- RAG 调研底稿只保留项目采用的结论、模型版本差异、评测边界和一手来源，课程负责展开教学；
- 路线图将简洁性加入每阶段固定门禁：删除重复、过时和脱离产品的说明，同时避免为降低行数制造更多跳转和抽象。

## M5 起点

当前没有阻塞进入 M5 的已知问题。任务取消、任务级重试/恢复、浏览器 E2E、迁移与公开部署保护仍是 M5 的实施内容，不应被描述为现有能力。
